<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="agent.ai.api.mapper.ChatMessageMapper">

    <!-- 批量插入聊天消息 -->
    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO chat_message (
            session_id, role, content, prompt_tokens, completion_tokens, total_tokens, metadata, created_at
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.sessionId}, #{item.role}, #{item.content}, 
                #{item.promptTokens}, #{item.completionTokens}, #{item.totalTokens}, 
                #{item.metadata}, #{item.createdAt}
            )
        </foreach>
    </insert>

</mapper>
